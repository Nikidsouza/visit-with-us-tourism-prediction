# Visit with Us Tourism Model

This repository contains a trained classification pipeline saved as `best_tourism_model.joblib`.

## Notes
- The model was trained using scikit-learn preprocessing and XGBoost.
- The artifact is serialized with `joblib`, which relies on Python pickle internally.
- Only load this model from trusted code and trusted environments.
